<% if @todo.valid? %>
  $('.js-todos').prepend("<%= escape_javascript(render(@todo)) %>");
  $('.new-todo').html("<%= escape_javascript(render partial: 'todos/form', locals: { user: @user, todo: @new_todo }) %>");
  $('.flash').after("<%= escape_javascript(render partial: 'layouts/flash') %>");
<% else %>
  $('.flash').after("<%= escape_javascript(render partial: 'layouts/flash') %>");
<% end %>