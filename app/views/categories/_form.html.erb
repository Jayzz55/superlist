<%= form_for [category] do |f| %>

  <div class="form-group">
    <%= f.label :body, 'Category' %>
    <%= f.text_area :body, rows: 4, class: 'form-control', placeholder: "Enter category" %>
  </div>

  <div class="form-group">
    <%= f.submit "Save", class: 'btn btn-success' %>
  </div>
  
<% end %>